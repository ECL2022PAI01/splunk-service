apiVersion: "spec.keptn.sh/0.2.0"
kind: "Shipyard"
metadata:
  name: "Splunk test shipyard"
spec:
  stages:
    - name: "production"
      sequences:
        - name: "deployment"
          task: 
            - name: "evaluation"